# 실행 계획
기준 커밋: 2d1f57d. 브랜치: feature/section-candidate-decisions.
1. 단일 판정 회귀 테스트를 먼저 작성하고 기존 코드의 실패를 확인한다.
2. 동적 schema와 검증기를 변경하고 기존 안전장치 및 호출 흐름을 테스트한다.
3. ROLE-002 문서에서 수작업으로 고정한 후보 풀을 사용하는 Solar 통합 호출로 변경 지점의 효과를 확인한다.
4. 동일 7사례로 section-v2를 평가하고 정답/오답/오류를 구분한다.
5. 코드 리뷰, 전체 테스트, 안전한 보고서 작성 후 commit/push한다.
## Review Focus
동적 ID 객체의 누락/미지ID, 빈 풀, 선택 순서, scalar/absence 충돌, 중복 JSON 키와 기존 검사 우회 여부.
실험 contract만 변경하며 신규 아키텍처 승인 범위를 확장하지 않는다.

평가 주의: 이전 실패에서 성공한 추출 응답은 보관 정책상 저장하지 않았다. 고정 후보 검사는 원래 풀 재생이 아닌 합성 통합 테스트이며 7사례 비교와 분리한다.

## 실행 기록
- T1: 신규 단일 판정/빈 풀 회귀 테스트가 기존 코드에서 SECTION_MERGE로 실패함을 확인.
- T2: 전체166/166 통과. source 후보 순서와 기존 필드 검증 유지.
- 최종 독립 리뷰: P1/P2 없음. 리뷰어가 중첩 JSON 중복 키 거부를 직접 확인.
- Minor 보류: decisions 중복 JSON 키 전용 영구 회귀 테스트는 없음. 공통 파서 검증과 리뷰 재현으로 현재 차단 확인.
