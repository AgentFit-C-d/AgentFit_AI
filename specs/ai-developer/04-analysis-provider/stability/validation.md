# 안정화 검증 — 2026-09-25

## 최종 결과

- profile-v4 / solar-pro4-260806 / reasoning=none / temperature=0.
- 사전 고정한 기존6건+새6건을 각각2회: **24/24 통과**. project_name 및 전체10필드가 기대값과 일치했다.
- 원래 실패 TEXT-SEM-002는 2/2 “예약 관리”로 일치했다. 변경 전 profile-v2 진단에서는 5/5 “예약 관리 프로젝트”로 불일치했다.
- 최종24회 지연은 0.687~8.093초. 응답 실패·구조 오류·재시도 0건. 이 작은 개발 표본의 관측값이며 운영 SLA가 아니다.
- 전체 단위 테스트 **29건 통과**. 평가회귀 구현 전 모듈부재 실패 확인. 새 Provider 계약 전 정상경로/근거경로 테스트 실패 확인 후 구현으로 통과.
- 결과: profile-v4-results.json. 이전 결과는 그대로 보존한다.

## 실패한 접근도 보존

| 버전/추론 | 통과 | 실패 |
|---|---:|---|
| v2 baseline (기존 실패사례만5회) | 0/5 | 이름 설명어 과포함5 |
| v3 medium | 14/24 | timeout10 |
| v3 low | 17/24 | timeout5, UNKNOWN_HAS_EVIDENCE2 |
| v3 none | 16/24 | UNKNOWN_HAS_EVIDENCE8 |
| v4 none (승인한 내부 구조 개선) | 24/24 | 없음 |

총101회 호출(진단5+고정24회*4). 이전 기능에서 실행한 실험과 별도다.
오류 응답의 토큰 사용량은 일부 미수집이므로 보고서 합계가 전체 청구량은 아니다.
시간에 따른 Provider 부하 차이가 혼입될 수 있으므로 추론 설정만으로 지연 차이 전부를 설명하지 않는다.

## 변경과 근거

이름 경계 규칙을 명확히 하고, 서로 떨어진 data/evidenceQuotes를 필드별 null 또는 {value,evidenceQuotes} 구조로 변경했다.
미정이면 필드 자체가 null이므로 근거를 붙일 수 없다. value=null인 객체는 거부한다.
Provider 변환기가 기존 내부 data/evidenceQuotes로 변환한 후 기존 원문/위치/Profile 검증을 적용한다.
사전 정답·검증기 조건을 바꾸거나 잘못된 값을 정답으로 치환하지 않았다.
Spring에 전달할 Profile 형식과 DB/HTTP 경계는 유지했다.

## 명세·계획·작업 교차 점검

- NS01/NS02: 기존 실패사례, 현재/과거 구분, 명시한 이름 속 “프로젝트” 보존 및 앱 이름 사례가 모두2/2 일치.
- NS03: 미정·후보·상충 null과 기존 근거 정책을 통과. UNKNOWN_HAS_EVIDENCE를 거부하는 기존 변환/검증 테스트도 유지.
- NS04/NS05: 최초 정답을 유지하고 사전24회 고정 평가. 세 미달 설정의 보고서도 보존.
- NS06: 일반 평가 보고서에는 원문/모델 이름 문자열 없이 위치와 메타데이터만 기록. baseline에는 고정 합성 이름만 있다. API 키/운영 원문은 기록하지 않았다.
- NS07: 사용자 승인 후 새 응답 Schema/adapter 구현. 실제 API 수용과 전체 필드 회귀 확인.
- 수동 기능별 SDD 교차 점검이다. 전역 Spec Kit CLI 전체 실행을 주장하지 않는다.

## 한계와 후속 범위

개발자가 사례를 알고 있으므로 독립 블라인드 평가가 아니다. 24회 성공은 모든 문서의 정확도 보장이 아니다.
FACT9/SEM12/NORMAL30 운영 품질 평가, PDF/FastAPI/Spring 통합, 실패 원본7일 저장은 아직 후속 범위다.
선행 feature/solar-profile-analysis에 의존하며 main 병합·배포는 수행하지 않는다.
