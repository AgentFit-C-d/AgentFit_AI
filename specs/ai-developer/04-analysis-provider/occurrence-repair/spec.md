# 기능 등장 순서 오류 보완
사용자 요청: 기존 품질 문제 보완. 범위는 기존 근거 검증·1회 수정 경로의 제한된 개선이다.
FR1: occurrence는 선택한 quote 자체가 지정 줄에 등장한 순번이다. 서로 다른 인용은 각각1부터 센다.
FR2: 검증 실패 시 수정 호출에 잘못된 span 인덱스와 해당 줄의 실제 일치 횟수를 전달한다. 원문 위치를 임의 보정하지 않는다.
FR3: 공개 Profile, Solar, 최대6회, 전체60초 예산, 실패 원본7일 정책을 유지한다.
SC1: 잘못된 순번을 재현하는 테스트가 먼저 실패하고 수정 후 통과해야 한다.
SC2: 고정 합성 사례와 기존 MABC 실패 초안 재생으로 수정 결과를 검증한다. 구조와 의미 점수를 구분한다.
SC3: 실제 문서 전체 분석의 의미 오류·시간 초과는 별도 잔여 문제로 기록한다. 국소 개선을 전체 품질 완료로 표현하지 않는다.
