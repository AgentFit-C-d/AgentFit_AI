# Solar 단계 분리 안정화 결과
최종 profile-v13 / solar-pro4-260806. 품질 게이트 미충족.
기반 a9963b6, 사용자 요청에 따라 독립 feature/staged-extraction-stability에서 진행.

## 채택한 수정
- 입력 JSON 줄 객체를 [L번호] 원문으로 단순화. 원문 위치 계산과 Profile 형식 유지.
- frequency_penalty=0 명시. none/4096토큰/40초/최대3호출/무재시도 유지.
- 기술 값으로 미정·없음 같은 상태 단어가 나오는 경우 거부.
- 인용된 모델 바로 뒤의 평가·운영 채택 검증후결정 구절을 좁게 검증하여 후보 모델을 거부.
- 검증 실패는 기존 1회 수정 절차로 넘김. 값을 자동 보정하거나 null로 덮어쓰지 않음.
- 가드는 일부 명시적 표현에 한정한다. 포괄적 의미 판정기가 아니며 외부 저장소 누락을 검출하지 못한다.
- 과잉 거부를 막기 위해 다른 모델의 결정이나 이미 완료된 채택을 분리하는 반례 테스트 포함.

## 최종 검증
전체 unittest 56/56 통과. 추가 테스트는 변경 전 실패를 확인함.
기존 합성12종×2회 23/24, 신규2건 2/2. 총25/26.
NAME-003 첫 반복에서 backend INVALID_EVIDENCE_LINE 수정후에도 실패. 두 번째 반복에서는 수정후 통과.
실제2문서×2회 3/4. HandOff 첫 반복에서 외부 백업 저장소 누락. AgentFit 첫 반복은 ai/features 수정후 통과.
v12(기존합성21/24, 신규2/2, 실제2/4)보다 이번 측정이 개선됐으나 운영 품질 인증은 아님.
v13 실제4회에는 네트워크 오류가 없었지만 이전 실험과 low 실험의 시간초과를 해결했다고 단정하지 않는다.
최종 부정 판정 범위를 좁힌 이후 전체 단위를 다시 통과했으며, 실측에 사용한 실제 후보 판정 구절은 동일하게 거부한다.

## 원인 조사와 미채택 실험
원래 none 재현에서 이름null 및 DB값 미정을 확인.
low는 단순합성 판단을 개선했으나 실제 문서에서 응답잘림/시간초과.
짧은 단계전용 프롬프트, 기존 quote 핵심추출, low+패널티0만으로는 기준 미달.
low/8192토큰/90초 대조는 핵심3/4 통과, HandOff 약80초후 PROVIDER_NETWORK. 기본설정으로 채택하지 않음.
단일 원인으로 확정하지 않는다. 비교는 진단용 핵심9필드만이며 전체 기능 평가와 구분한다.
공식 API에서 frequency_penalty 기본1.1과 reasoning/max_tokens 동작을 확인:
https://console.upstage.ai/api/chat (2026-09-25 조회).

## 다음 결정
사용자 “OpenAI 비교 평가 진행” 승인. 같은 문서와 정답, 동일 Profile 검증기로 별도 feature에서 비교한다.
Solar 운영 채택이나 자동 병합/배포는 미실행.
실제 문서/Profile은 로컬 output만, Git에는 합성자료와 내용 제외 결과만 보존.
