# 계획
1. NAME-003의 최초·수정 출력을 합성 데이터로 재현한다.
2. 현재 스키마는 확정 필드의 evidenceLineIds 빈 배열과 존재하지 않는 id를 허용한다. 실제 요청 스키마에 비어 있지 않은 근거와 현재 문서의 줄 범위를 반영한다. features의 존재/없음 분기도 구조로 제한한다.
3. TDD로 요청 스키마와 서버 검증의 방어를 확인한다. JSON Schema를 더 엄격하게 하며 검증기는 완화하지 않는다.
4. 고정 전체 평가. 외부 저장소 누락이 남으면 생성 순서/근거 우선 등 한 변수만 별도 대조한다. 프롬프트/기준을 임의 누적변경하지 않는다.
5. 검증 결과/한계를 기록하고 feature 브랜치 commit/push. 운영 채택/병합/배포는 별도.

## 재현과 구현
v13 NAME-003 첫 시도의 backend는 FastAPI지만 evidenceLineIds=[2]였다. 실제 문서는1줄. 수정 후[1]로 통과했고 둘째 시도는 처음부터[1]. 원인은 문장 번호와 줄 번호 혼동을 schema가 허용하는 것이다.
profile-v14: 요청마다 실제 줄 수를 maximum에 넣고, 확정 core근거는 minItems1. features는 spans≥1/absence=[] 또는 spans=[]/absence≥1 두 분기로 제한. 서버검증 유지.
단위59통과, 기존합성24/24(모두최초검증), 신규합성2/2. 실제 문서평가진행중.

## 기능 구간 진단 후 v15
v14 합성26/26이지만 실제1/4. 별도 기능진단에서 AgentFit26구간 중1개는 시작/끝 모두 원문에 없고2개는 경계단어 반복으로 모호했다. HandOff진단21구간은 정상.
기능 내부 span을 {lineId,quote}로 단순화한다. quote는 선택한 줄에서 정확히1회 나타나는200자이하 연속문자열이어야 하며, 서버는 해당 구간을 그대로 복사한다. 분리된 시작/끝 단어 생성 및 조합을 제거한다. 근거완화/퍼지매칭/자동값보정 없음. Profile문자열배열/offset형식 유지.

## v16 외부 연동 분류
v15 합성26/26, 실제3/4. 기능 인용 검증은 모두 통과했으나 HandOff 반복2에서 외부 백업 저장소 누락. core 단독 분류 실험은 두 문서×2회 모두 고정 core 기준 통과. 외부 연동 value를 authentication/notifications/storage/other의 필수 배열 객체로 추출하고 서버가 순서 유지·중복 제거 후 기존 배열로 합친다. 모든 서비스는 기존 원문 근거 검증을 통과해야 한다. 특정 서비스명 주입 없음. 미언급은 null, 명시적 없음만 모든 분류 빈 배열+근거. 호출수와 공개 Profile 유지.

## 최종 결과
v16 단위66/66, 합성26/26, 실제2/4. 외부 연동 분류 단독 성공이 전체 품질 개선으로 이어지지 않았다. 실험 브랜치로 보관하고 추가 수정 전 프롬프트 분리 설계를 검토한다. 상세는 validation.md.
