# 신규 README 평가 — 2026-09-25

## 평가 조건
사용자가 지정한 README 한 파일(2,106 code points / UTF-8 3,884 bytes / 53줄).
참조 문서·명령 미실행. profile-v18 고정, 호출별 진단 활성화,2회,네트워크재시도0.
로컬 rubric을 응답 전에 고정했으며 결과파일에 source/rubric SHA256 및 고정시각을 기록했다.
정답은 개발자 임시 기준이며 사용자가 승인한 골드는 아니다. 운영 AI 여부는 보수적으로 해석했다.

## 결과
- 의미 기준 통과0/2.
- 구조·근거 검증은2/2이나, 모두 기능 인용 오류를1회 수정한 뒤 성공했다. 최초 검증 성공0/2.
- 각각 총3호출, 전체6호출. timeout0. 단순 구조검증 성공을 의미 정확도로 집계하지 않았다.
- 추출 코드·프롬프트 변경 없음. 평가 문서 추가만이므로 단위·기존 전체 품질평가를 재실행하지 않았다.

| 회차 | core | features | repair | 전체 |
|---|---:|---:|---:|---:|
| 1 | 5.656초 | 19.360초 | 10.968초 | 35.984초 |
| 2 | 2.063초 | 5.843초 | 3.172초 | 11.078초 |

이번 표본의 지연 대부분은 기능 추출+인용수정이었다. transport 대기시간이므로 모델 생성·네트워크 지연을 분리한 수치는 아니다. 과거 timeout 원인을 이것만으로 단정하지 않는다.

## 자동 고정 기준 불일치 (두 회차 공통)
- project_type: MCP 서비스 형태가 명시됐지만 null.
- ai: MCP 클라이언트까지 모델 목록에 포함. 개발·시연 모델을 운영 채택으로 포함. 클라이언트 분류 오류는 명확하며 모델 용도는 rubric의 보수적 기준에 따른 불일치.
- external_integrations: 회차1은 빈 배열(명시적 없음), 회차2는 제공자 이름 없는 일반 교통 데이터/API 표현. rubric은 특정 외부 서비스 미언급을 null로 정했다.

## 수동 검토
프로젝트 이름, 확정 계산 백엔드와 배포 대상, 핵심 기능 개념은 찾았다.
그러나 backend에 배포 플랫폼까지 섞였고, 기능 목록에 기술구성 설명·팀원 분업·개발검증 계획·폴더 설명을 포함했다.
필수 기능의 개념은 포함되지만 여러 동작을 한 긴 문구로 합쳤다. 자동 필수개념 검사는 이런 추가 오분류를 잡지 못하므로 수동 결과를 별도로 기록했다.
기능 인용 수정 전 응답은 최종 구조검증이 성공했기 때문에 기존 실패 원문 보관 정책상 저장하지 않았다. 초기 오류 코드는 INVALID_FEATURE_SPAN으로 확인했지만 정확히 어떤 구간이 틀렸는지는 보존된 진단만으로 확정할 수 없다.

## 후속 개선 우선순위
1. 서비스 형태와 도구·클라이언트·운영 모델·개발/시연 모델을 구분하는 일반 기준 및 합성 회귀 사례.
2. 기능 후보에서 폴더 설명·팀 분업·개발 절차·기술선택을 제외하고 사용자/운영 동작만 남기는 기준.
3. 외부 연동의 미언급(null)·명시적 없음([])·이름 없는 데이터 소스를 분리.
4. 백엔드 기술과 배포/컨테이너 분류를 정리하고 기존 Profile 필드 제약 내 일관성 검증.
이 문서는 이제 실패 분석에 사용했으므로 이후 튜닝 시 미사용 평가 문서로 취급하지 않는다. 개선 후 별도 신규 문서로 검증해야 한다.

## 보관
원문, 전체 Profile, rubric은 Git에서 제외된 로컬 output/mabc-evaluation에만 있다.
저장소에는 평가계획·비식별 결과·이 보고서만 기록했다. 진단 저장소의 두 실행은 구조상 성공이므로 실패 응답 원문0건이며 메타데이터만 저장한다.
