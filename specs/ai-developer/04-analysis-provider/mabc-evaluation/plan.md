# 새 README 평가 계획
사용자가 지정한 README 한 파일만 입력한다. 참조 링크·명령은 실행하지 않는다.
기준: bec54dd/profile-v18. 프롬프트·스키마·코드 변경 없이 신규 문서 평가.
API 호출 전에 로컬 rubric을 고정하고 SHA256/UTC시각 기록. 동일 문서2회, 재시도 없음, 호출별 진단 활성화.
기대: MCP 제품 형태, 확정 백엔드·배포, 장소/사용자확인/출발시각/재탐색/선택 기능. 개발·시연 모델을 운영 AI로 채택하지 않는다. 클라이언트/이전검토폴더는 현재 스택으로 넣지 않는다.
Docker는 전용 필드가 없어 backend/deployment 중 하나에서 수용. 모델 용도 판정은 보수적 개발자 임시 기준이며 사용자확정 정답 아님.
원문·Profile·rubric은 로컬 output에만 보관하고 저장소에는 결과·메타데이터만 기록.
구조검증/의미판정/최초·수정성공/호출시간을 구분한다. 실패를 제외하지 않는다.
평가 후 수동검토하고 결과기록/브랜치push. 이번 요청은 평가이며 추출기 수정은 포함하지 않는다.
