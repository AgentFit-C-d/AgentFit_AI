# 검증 결과 — 단계 분리 구현, 품질 기준 미달
2026-09-25. 최종 profile-v12 / solar-pro4-260806 / reasoning none.
사용자 승인: 기술·연동과 기능 분리, 실패 필드만 1회 수정, 최대3회 호출.

## 최종 결과
- 단위 테스트 48/48 통과. 정상2호출, 다중 실패도 수정1호출, 수정후 실패 종료, 타임아웃 무재시도, 유니코드/CRLF 근거, 민감정보 비노출, 토큰 합산 포함.
- 기존 합성12종×2회: 21/24 정답. 최초 구조검증23/24, 수정후 구조검증1/24. 실제 API49호출.
- 신규 기능/예시 구분 합성: 2/2 정답, 모두 최초 검증 통과(4호출).
- 실제2문서×2회: 2/4 정답(두 번 모두 AgentFit). 최초 구조검증2/4, 수정후 구조검증1/4, 시간초과1/4. HandOff는 백업 저장소 누락1회, PROVIDER_TIMEOUT1회. 성공 응답 지연14.4~24.7초, 시간초과51.2초. 실제 API9호출.
- 실제 문서 판정은 사전 개발자 임시 rubric이며 사용자 확정 정답/운영 인증이 아니다.
- 최종 문서당2회 추출, 구조/근거 검증 실패시에만1회 수정. 의미상 누락·잘못된 확정 판단을 자동 검출하는 검증기는 아니다.

## 남은 문제
v12에서 TEXT-FACT-001은 이름을 null로 반환(2회), NAME-006은 database 오판(1회). v10의 일반 설명어 포함 오류는 기존 v4 이름 규칙을 복원한 v12에서 재현되지 않았지만 다른 회귀가 남았다.
기존 이름 안정화24/24 대비 회귀이므로 품질 게이트는 미통과다.
profile-v11 이름 schema 설명 추가는 효과가 없어 채택하지 않았다. v12는 diff에서 발견한 이름 규칙 축약을 v4의 규칙으로 복원한 버전이다. 실패 보고서를 모두 보존한다.
실제 API시간초과는 원인이 확정되지 않았다. 네트워크 재시도나 총60초 SLA를 구현했다고 주장하지 않는다.
실패 원본7일 저장, HTTP/PDF/Spring 통합은 미구현이다.

## 실험 이력
|버전|신규 합성|실제 내용 기준|주요 결과|
|---|---|---|---|
|v5|2/2|0/4|기능 의역/반복 근거|
|v6|2/2|0/4|기능 개수 초과·원문 불일치·시간초과|
|v7|1/2|0/4|구조4/4 통과, 모델 후보/외부 저장소 오판|
|v8|2/2|0/4|구간 오류3회·저장소 누락1회|
|v9|2/2|1/4|구조4/4 통과, 의미 오류3회|
|v10|2/2|3/4|구조 성공3회·시간초과1회|
|v12|2/2|2/4|구조 성공3회(수정1회 포함)·시간초과1회, 저장소 누락|

기존 합성은 v5 24/24, v10 20/24, v11 20/24, v12 21/24. v6~v9는 기존24회 평가를 실행하지 않았다.
실제 원문/Profile은 로컬 output/document-quality에만 있다. Git에는 코드, 합성 입력, 내용 제외 결과를 기록한다.
v10 실제 실행 중 추가한 실패 호출수 메타데이터는 해당 시간초과 행에 없으므로 호출수를 추정해 채우지 않았다.
verification-before-completion 기준으로 최종 테스트와 diff를 확인했다. SDD는 이 폴더의 spec/plan/tasks에 기록했으며 전체 Spec Kit CLI 실행을 의미하지 않는다.
